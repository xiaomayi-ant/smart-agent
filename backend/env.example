#LLM（openai）
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-xxdfs
LLM_MODEL=gpt-4o
STRUCTURED_PLANNER_METHOD=auto

# LLM（DeepSeek）
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-856657
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat
STRUCTURED_PLANNER_METHOD=auto

# 仅测试通信：关闭意图路由，避免下载HanLP
INTENT_ENABLED=false

# Server
HOST=0.0.0.0
PORT=3001
DEBUG=true
CORS_ORIGINS=["*"]

# MySQL：给占位值以绕过导入时的必填校验（不会真正连上）
MYSQL_HOST=47.251.1
MYSQL_PORT=3306
MYSQL_USER=sux
MYSQL_PASSWORD=Sx
MYSQL_DATABASE=bux

# Milvus：留空即可（代码会自动降级为禁用）
MILVUS_ADDRESS=sx:19530
MILVUS_SSL=false

#silicon：兼容代理商
OPENAI_EMBED_API_KEY=skx-dsbkcsshotrqmazslyqsinwag
OPENAI_EMBED_BASE_URL=https://api.siliconflow.cn/v1
OPENAI_EMBED_MODEL=Qwen/Qwen3-Embedding-0.6B
OPENAI_EMBED_DIM=1024

# Vector store collection name (allow parallel collections for different dims)
DOCUMENTS_COLLECTION_NAME=documents_1024
RESET_DOCUMENTS_COLLECTION_ON_STARTUP=false

# Web search (Tavily)
TAVILY_API_KEY=tvlyx-JUMRXL6iN


# PostgreSQL (for thread persistence)

PG_DSN=postgresql://sumoer:Sux$@x:5432/chat_history?sslmode=require

# Voice / ASR (Dictation mode)
# Enable to register /api/asr endpoints
ENABLE_VOICE=true
# Whisper model size: tiny, base, small, medium, large
WHISPER_MODEL=base
# Force ASR language (e.g., zh, en). Leave empty for auto-detect
ASR_LANGUAGE=


#Neo4j
# Neo4j / Graphiti configuration
# Provide connection info for graph operations/tools. Keep credentials safe.
NEO4J_URI=boltx://x:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=Sux
NEO4J_DATABASE=neo4j

#JWT
JWT_SECRET=yx

#DEBUG_GRAPH
DEBUG_GRAPH_EVENTS=0

#TRACE
TRACE_EVENTS=true
